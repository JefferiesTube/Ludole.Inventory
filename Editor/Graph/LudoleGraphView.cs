using GraphProcessor;
using UnityEditor;

namespace Ludole.Inventory.Editor
{
    public class TooltipGraphView : BaseGraphView
    {
        public TooltipGraphView(EditorWindow window) : base(window)
        {
            
        }
    }
}